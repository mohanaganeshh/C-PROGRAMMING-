DAY 5 - max number in array

#include <stdio.h>
int main()
{
   int i,max=0,a[5];
   printf("Enter 5 numbers : ");
   for(i=0;i<5;i++)
   {
       scanf("%d",&a[i]);
       if(a[i]>max)
       {
           max=a[i];
       }
       
   }
   printf("Max = %d",max);
    return 0;
}