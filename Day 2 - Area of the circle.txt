#include <stdio.h>
int main() 
{
    float r,a;
    printf("Enter the radius of the circlr : ");
    scanf("%f",&r);
    a=3.14*r*r;
    printf("the area of the circle is %f",a);
    return 0;
}