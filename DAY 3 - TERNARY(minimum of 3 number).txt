DAY 3 - TERNARY(minimum of 3 numbers)

#include <stdio.h>

int main() {
    int min,a,b,c;
    printf("Enter three numbers : ");
    scanf("%d %d %d",&a,&b,&c);
    min=(a<b)?((a<c)?a:c):((b<c)?b:c);
    printf("Minimum of three numbers = %d\n",min);
    return 0;
}